function buildTrie(...words) {
    console.log(words.length)
    let objectTrie = {}
    if (words.length === 0) {
        return objectTrie
    } else if (words.length === 1 && words == "") {
        return objectTrie
    }
}